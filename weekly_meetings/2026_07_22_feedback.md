### Progress and issues discussed

- The project should use one multimodal model initially and focus on building a reliable pipeline before comparing models.
- The pilot observations cannot be assessed only by manually reading the generated responses.
- A reproducible NLP analysis pipeline is required.
- Candidate measures mentioned included sentiment, adjectives, unsupported inferences and semantic similarity.
- Each measure must be scientifically defined and connected to a hypothesis.
- Progress was considered behind schedule, and a substantial implementation result is expected at the next meeting.

### publication potential

At this rate, the dissertation shapes up to be a standard degree dissertation, yet falls short of the standards required for scientific research.

Progress has been slow so far, and the pace needs to be stepped up.

### To-do work by next week

1. Semantic shift. Compare each country-conditioned description with the no-country control using sentence embeddings:

$$ D_{i,c} = 1-\cos\left( e(y_{i,c}), e(y_{i,0}) \right)$$

where $i$ is the image and $c$ is the country condition.

2. Extract adjectives and compare their frequencies across country conditions. The student should use a small, fixed set of categories, for example:

- appearance; professionalism or competence;
- warmth;
- modernity or traditionalism;
- socioeconomic status.

3. Unsupported inference rate

Define a simple binary outcome:

$$U_{i,c} =
\begin{cases}
1, & \text{if the output introduces an occupational, personality,}\\
   & \text{religious, socioeconomic or cultural claim not visible in the image}\\
0, & \text{otherwise}
\end{cases}$$

For the pilot, this could be manually coded using a written protocol. There is no need yet to build an automatic classifier.

Other points:

- Freeze the experimental design. One model: Gemini. One prompt template. One no-country control. Four country labels for the pilot.
- Run a proper pilot. At least 10 images. Five conditions per image: control plus four countries. Repeat every condition three times.
- Implement semantic distance. Compute embedding distance between every country-conditioned response and its control.
- Extract and compare adjectives. Produce a table of adjective frequencies by country condition.
- Create a simple unsupported-inference coding scheme. Manually label occupation, personality, socioeconomic, religious and cultural assumptions. Report the inference rate by condition.
- Produce results, not only code. At least two figures. At least two summary tables. Several concrete examples showing meaningful changes.
- Upload the complete reproducible pipeline to GitHub. Prompts; raw responses; analysis notebook; figures; weekly slides.
